{
  "buildCommand": "cd .. && pnpm --filter aetherpulse-client build",
  "outputDirectory": "dist",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
