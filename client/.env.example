# The port the Vite dev server should run on
PORT=5173

# The base path for the application
BASE_PATH=/

# Optional: Server URL if the API is on a different host
# VITE_API_URL=http://localhost:3000/api
